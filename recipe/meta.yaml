{% set name = "oirfile" %}
{% set version = "2026.7.28" %}
{% set python_min = "3.12" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 93a7dc7505f420ea4ff5f99e56f96e4547a1c0cdcfd520c2d0d5912c4bc135ad

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv
  noarch: python
  entry_points:
    - oirfile = oirfile.oirfile:main

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - matplotlib-base
    - numpy
    - python >={{ python_min }}
    - tifffile
    - xarray

test:
  imports:
    - oirfile
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://www.cgohlke.com/
  summary: Read Olympus/Evident OIR and POIR files
  license: BSD-3-Clause
  license_file: LICENSE
  description: |
    Oirfile is a Python library to read images and metadata from OIR (Olympus Image Format Raw) files and POIR archives (ZIP collections of OIR files) produced by Olympus/Evident FluoView fluorescence microscopy software.
  dev_url: https://github.com/cgohlke/oirfile/

extra:
  recipe-maintainers:
    - cgohlke
